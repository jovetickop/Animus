{
  "Stop": [
    {
      "matcher": "*",
      "hooks": [
        {
          "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/linear-sync.sh\" 2>/dev/null || powershell -ExecutionPolicy Bypass -File \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/linear-sync.ps1\" 2>/dev/null || exit 0",
          "timeout": 10,
          "type": "command"
        }
      ]
    }
  ]
}
